/* =========================================================
   main.js — start-up calls (runs last, after every file above
   has defined its functions)
   ========================================================= */
renderFileList();
runDiagnostics();
